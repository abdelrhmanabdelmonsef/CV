{
  "name": "cv-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "libs/*"
  ],
  "scripts": {
    "dev:web": "npm run dev -w web-cv",
    "dev:api": "npm run dev -w api-cv",
    "sync-assets": "node scripts/sync-assets.mjs",
    "generate-pdf": "node scripts/generate-resume-pdf.mjs",
    "build:cv-data": "npm --workspace cv-data run build",
    "build:vercel": "npm run build:cv-data && npm run sync-assets && npm run generate-pdf && npm run build -w web-cv",
    "predev": "npm run build:cv-data && npm run sync-assets && npm run generate-pdf",
    "prebuild": "npm run build:cv-data && npm run sync-assets && npm run generate-pdf",
    "build": "npm --workspace ./apps/web-cv run build && npm --workspace ./apps/api-cv run build",
    "start:api": "npm --prefix ./apps/api-cv run start:prod"
  },
  "devDependencies": {
    "concurrently": "^10.0.3",
    "typescript": "^5.6.0"
  }
}
