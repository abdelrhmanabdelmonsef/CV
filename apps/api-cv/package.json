{
  "name": "api-cv",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "predev": "npm --workspace cv-data run build",
    "dev": "nest start --watch",
    "prestart:dev": "npm --workspace cv-data run build",
    "start:dev": "npm run dev",
    "prebuild": "npm --workspace cv-data run build",
    "build": "nest build",
    "start:prod": "node dist/main.js"
  },
  "dependencies": {
    "cv-data": "*",
    "@nestjs/common": "^10.3.0",
    "@nestjs/core": "^10.3.0",
    "@nestjs/platform-express": "^10.3.0",
    "reflect-metadata": "^0.1.13",
    "rimraf": "^5.0.0",
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.1"
  },
  "devDependencies": {
    "@nestjs/cli": "^10.3.0",
    "@types/express": "^4.17.18",
    "typescript": "^5.6.0",
    "ts-node": "^10.9.1"
  }
}
